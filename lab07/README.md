# Отчёт по лабораторной работе

## Марковская модель погоды

### 1. Цель работы
Изучить принципы построения и анализа дискретных цепей Маркова. Реализовать программное приложение для имитационного моделирования динамики состояний погоды с графическим интерфейсом для:
1. Моделирования переходов между тремя состояниями (ясно, облачно, пасмурно) на основе матрицы переходных вероятностей.
2. Визуализации процесса в реальном времени.
3. Расчета теоретического стационарного распределения и сравнения его с эмпирическими данными.
4. Сбора и хранения статистики в формате `.csv`.

---

### 2. Описание приложения

#### 2.1. Модуль настройки и управления
Приложение позволяет пользователю интерактивно задавать вероятности переходов в виде матрицы $3 \times 3$. Реализована проверка условия нормировки. Также предусмотрен регулятор скорости генерации для визуального анализа процесса на разных временных масштабах.

#### 2.2. Система визуализации и логирования
Интерфейс разделен на три функциональные зоны:
- **Информационная панель:** отображает текущий день и текущее состояние системы с использованием графических символов.
- **График истории:** линейная диаграмма последних 100 переходов, позволяющая отследить динамику изменений.
- **Панель сравнения:** гистограмма, в реальном времени сопоставляющая накопленные частоты с математическим ожиданием.

Все данные автоматически записываются в файл `weather_analysis.csv`, который обнуляется при нажатии кнопки «Сброс».

---

### 3. Вероятностная модель

#### 3.1. Дискретная цепь Маркова
Математическая модель описывается множеством состояний $S = \{1, 2, 3\}$ и матрицей переходных вероятностей $P$:
$$P = \begin{pmatrix} p_{11} & p_{12} & p_{13} \\ p_{21} & p_{22} & p_{23} \\ p_{31} & p_{32} & p_{33} \end{pmatrix}$$
где $p_{ij} = P(X_{t+1} = j \mid X_t = i)$ — вероятность перехода в состояние $j$ при условии, что сейчас система находится в состоянии $i$.

#### 3.2. Стационарное распределение
Теоретическое стационарное распределение $\pi = (\pi_1, \pi_2, \pi_3)$ находится из системы линейных уравнений:
$$\begin{cases} \pi P = \pi \\ \sum \pi_i = 1 \end{cases}$$
Для решения в приложении используется матричный метод решения СЛАУ: $(\mathbf{P}^T - \mathbf{I})\pi = 0$ с заменой одного уравнения на условие нормировки.

---

### 4. Графический интерфейс пользователя

Интерфейс реализован на библиотеке `CustomTkinter` в темной теме.

![Uploading image.png…]()

**Рисунок 1** — Основное окно программы в процессе моделирования

---

### 5. Результаты моделирования

В качестве тестовых данных использовалась матрица:
- Ясно: $[0.7, 0.2, 0.1]$
- Облачно: $[0.3, 0.4, 0.3]$
- Пасмурно: $[0.2, 0.3, 0.5]$

Теоретическое: 0.457, 0.283, 0.261

#### Сравнение распределений при разном количестве дней

| Объем выборки ($N$) | Эмпирическое   | Погрешность(%)     |
|---------------------|----------------|--------------------|
| 10                  |0.7, 0.2, 0.1   | 53.17, 29.33, 61.69|
| 20                  |0.4, 0.35, 0.25 | 12.47, 23.67, 4.21 |
| 50                  |0.32, 0.34, 0.34| 29.98, 20.14, 30.27|
| 100                 |0.43, 0.3, 0.27 | 5.91, 6.01, 3.45.  |

*Примечание: Теоретические значения рассчитываются один раз при задании матрицы, эмпирические — уточняются с каждым новым днем.*

---

### 6. Выводы

1. **Сходимость:** Подтверждена эргодическая теорема для цепей Маркова: при достаточно большом количестве шагов эмпирические частоты состояний погоды практически полностью совпадают с теоретическим стационарным распределением.
2. **Влияние матрицы:** Изменение диагональных элементов матрицы напрямую влияет на «вязкость» модели: высокие значения $p_{ii}$ приводят к длительным периодам сохранения одной и той же погоды.
