
# Отчет по лабораторной работе №5
## Тема: Моделирование случайных событий

---

### 1. Цель работы
Изучить и реализовать на практике методы моделирования случайных величин. Разработать программный комплекс, включающий:
1.  **Модель европейской рулетки** с использованием алгоритма кумулятивной суммы вероятностей.
2.  **Бинарный генератор («Да/Нет»)** на основе распределения Бернулли.
Обеспечить визуальную синхронизацию математической модели с графическим интерфейсом на базе библиотеки `tkinter`.

---

### 2. Описание приложений

#### 2.1. Игровой модуль «Рулетка»
Программа имитирует работу стандартного европейского стола:
* **Колесо:** 37 секторов (0 — зеленый, 18 красных, 18 черных).
* **Ставки:** Реализованы 2 типа
  
| Тип ставки | Количество чисел | Выплата (чистая прибыль) | Общий возврат в коде | Шанс успеха |
| :--- | :--- | :--- | :--- | :--- |
| **На число** | 1 | 35:1 | **x36** | 2,7% |
| **На цвет** | 18 | 1:1 | **x2** | 48,6% |


* **Визуализация:** Круговая диаграмма на объекте `Canvas`, вращающаяся с заданным ускорением и инерционным замедлением.

#### 2.2. Модуль «Да / Нет»
Интерфейс для получения быстрых ответов на бинарные вопросы. Работает независимо от игрового модуля и использует упрощенную вероятностную модель.

---

### 3. Вероятностные модели и программная реализация

#### 3.1. Алгоритм выбора числа (Метод кумулятивной вероятности)
В отличие от прямого выбора из списка, в работе реализован метод, имитирующий распределение на отрезке $[0, 1)$. 
Пусть $M = 37$ (количество секторов), тогда вероятность каждого исхода $p_i = 1/37$. Программа генерирует случайное число $\alpha \in [0, 1)$ и ищет интервал, в который оно попало.

**Математическая модель:**
$$P(X = k) \text{ если } \sum_{i=1}^{k-1} p_i \le \alpha < \sum_{i=1}^{k} p_i$$

**Фрагмент кода реализации:**
```python
m = len(self.wheel_order)
p_i = 1.0 / m                # Вероятность одного исхода
alpha = random.random()      # Случайная точка на отрезке [0, 1)
cumulative_p = 0.0

for k in range(m):
    cumulative_p += p_i
    if alpha < cumulative_p:
        win_num = self.wheel_order[k]
        break
```


#### 3.2. Модель «Да / Нет»
Для бинарного выбора используется функция сравнения с порогом $p = 0.5$. Это простейший случай распределения Бернулли, где вероятность успеха и неудачи равны.
```python
def get_yes_no(self, p=0.5):
    return "ДА" if random.random() < p else "НЕТ"
```

#### 3.3. Тригонометрическая синхронизация
Для того чтобы стрелка указывала точно на выбранное число, индекс выигравшего числа ($win\_idx$) пересчитывается в градусы:
$$\text{Финальный угол} = win\_idx \times \frac{360^{\circ}}{37}$$
Это гарантирует соответствие графического результата внутренним вычислениям.

---

### 4. Выводы
В ходе выполнения лабораторной работы был разработан программный комплекс на языке Python, объединяющий игровую модель европейской рулетки и систему бинарных предсказаний.
